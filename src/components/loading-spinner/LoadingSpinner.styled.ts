import styled from 'styled-components';

export const LoadingSpinnerContainer = styled.div`
  position: absolute;
  left: 0;
  right: 0;
  text-align: center;
  margin-top: 10px;

  .spinner-grow {
    margin-right: 10px;
  }
`;